<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.commerce.huayi.mapper.ContractInfoMapper" >

    <select id="getContractInfo" resultType="com.commerce.huayi.entity.db.ContractInfo">
        select id as id,
             content as content,
             del_flag as delFlag,
             create_date as createDate,
             update_date as updateDate
        from tb_contract_info
        where del_flag = '0'
    </select>

    <insert id="addContractInfo">
        insert into tb_company_info (content,del_flag,create_date) values
            (#{contractInfo.content, jdbcType=VARCHAR},#{contractInfo.delFlag, jdbcType=VARCHAR},#{contractInfo.createDate, jdbcType=INTEGER})
    </insert>

    <update id="delContractInfo">
        update tb_company_info set del_flag = '1',update_date = now() where id = #{id, jdbcType=INTEGER}
    </update>


</mapper>